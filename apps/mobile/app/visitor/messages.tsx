/**
 * Mensajes de quien todavía no tiene ficha: la misma pestaña que la del alumno.
 *
 * Ya sabía atender a esta cuenta —sus hilos se piden con la credencial que haya
 * (`myConversations`)— y lo que le faltaba era un sitio fijo. Antes vivían como
 * una sección del directorio, y quien le escribía a un gimnasio no tenía dónde
 * volver a mirar si le contestó.
 */
export { default } from '../student/messages';
