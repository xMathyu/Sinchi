node_modules/
dist/
build/
coverage/
.turbo/
*.tsbuildinfo
.tmp/

# Expo regenera estas carpetas con `expo prebuild` desde app.json (ver README §Convenciones).
# Versionarlas arrastra configuracion especifica de maquina.
apps/mobile/ios/
apps/mobile/android/
apps/mobile/.expo/

.next/
# Next exporta la landing estatica a apps/web/out
out/
# Next lo reescribe apuntando a .next/dev o a .next segun el comando: versionarlo
# deja el arbol sucio cada vez que se levanta el servidor.
apps/web/next-env.d.ts
.nest/

.env
.env.*
!.env.example
# Por lo mismo que los `.env`: lleva una clave `AIza…` (ver `apps/mobile/app.config.ts`).
apps/mobile/google-services.json

# Lienzos de diseño (/design). Son archivos de trabajo, no del producto: cada
# .html empaquetado lleva el editor dentro y pesa ~2 MB, y lo que vale de verdad
# —el diseño— ya vive publicado en su enlace.
.design/

.DS_Store
Thumbs.db
*.log
npm-debug.log*
